---
"job-hunter": minor
---

Add profile flags `location.strict_location_fit` and `location.strict_work_type` (default off). When enabled, collect and outreach hard-drop location/work-type mismatches instead of only soft-scoring them.
